{
  "scripts": {
    "test": "hardhat test",
    "solhint": "./node_modules/.bin/solhint -f tap contracts/tr3x.sol",
    "solfmt": "./node_modules/.bin/prettier --write contracts/tr3x.sol",
    "solc": "npx hardhat compile",
    "deploy": "npx hardhat run --network moonbase scripts/deploy.js",
    "cockpit": "npx hardhat console --network moonbase",
    "fmt": "npx prettier --no-semi --arrow-parens avoid --trailing-comma none --print-width 80 --write './*/**.js'"
  },
  "dependencies": {
    "chai": "^4.2.0",
    "hardhat": "^2.0.4"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "ethereum-waffle": "^3.3.0",
    "ethers": "^5.1.0",
    "prettier": "1.19.1",
    "prettier-plugin-solidity": "1.0.0-beta.4",
    "solhint": "3.3.2",
    "solhint-plugin-prettier": "0.0.5"
  }
}
